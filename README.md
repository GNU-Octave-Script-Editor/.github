# .github
GNU Octave Script Editor and GNU Octave Command Window keep numeric work in files you own. GNU Octave Plot Figure is a figure you can still save. Keep the workspace readable, add a package only for a gap you can name, pin seeds when a run must repeat, and commit the script not a dump.
